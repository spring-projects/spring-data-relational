CREATE TABLE SIMPLE_ENTITY
(
    ID   BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY,
    NAME VARCHAR(100)
);

CREATE TABLE WITH_LIST
(
    ID   BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY,
    NAME VARCHAR(100)
);

CREATE TABLE CHILD
(
    WITH_LIST   BIGINT REFERENCES WITH_LIST(ID),
    WITH_LIST_KEY INT,
    NAME VARCHAR(100)
);